import { Inter, Permanent_Marker, Zeyada } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const zeyada = Zeyada({ weight: "400", subsets: ["latin"] });
export const permanentMarker = Permanent_Marker({ weight: "400", subsets: ["latin"] });
